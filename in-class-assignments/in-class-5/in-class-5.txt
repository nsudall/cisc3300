1. Hex values represent values for red, green, and blue in hexadecimal code
2. Typography can be defined as “the art an technique of arranging type in order to make language visible”
3. The font-face rule sets a new font to use that is made via font family
4. list-style shorthand property allows you to set all the list style properties at once
5. The obje CSS property sets how the content of a replaced element, such as an <img> or <video>, should be resized to fit its container